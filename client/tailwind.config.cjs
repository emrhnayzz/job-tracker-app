/** @type {import('tailwindcss').Config} */
module.exports = {
  // Define paths to all template files where Tailwind classes will be used
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {
      // Custom theme extensions (colors, fonts, etc.)
    },
  },
  plugins: [],
};
